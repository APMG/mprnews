# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

gem 'capistrano', '~> 3.10'

source 'https://rubygems.publicradio.org/' do
  gem 'capistrano-apm', '~> 2.4'
end
