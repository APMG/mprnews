import React from 'react';
import PropTypes from 'prop-types';
import '../styles/index.scss';

const TwitterLayout = ({ children }) => <>{children}</>;

TwitterLayout.propTypes = {
  children: PropTypes.any
};

export default TwitterLayout;
